<head>
 <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>


<form class="text-center border border-light p-5" action="{{url_for('login')}}" method="POST">

    <p class="h4 mb-4">Přihlášení</p>

    <input type="text" name="username" class="form-control mb-4" placeholder="Jméno:">

    <!-- Password -->
    <input type="password" name="password" class="form-control mb-4" placeholder="Heslo:">


    <!-- Sign in button -->
    <input class="btn btn-info btn-block my-4" type="submit" value="Přihlášení">
    <div>
         <p>{{error}}</p>
        </div>

    <!-- Register -->
    <p>Nemáš účet?
        <a href="{{url_for('register')}}">Registrace</a>
    </p>

    <!-- Social login -->
    <p>Zpátky na:</p>
      <a href="{{url_for('index')}}">Home</a><br>
      <a href="{{url_for('info')}}">Info</a>
</form>
